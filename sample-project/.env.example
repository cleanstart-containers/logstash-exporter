# Logstash monitoring API URL (default: http://localhost:9600)
# When running in Docker, use host.docker.internal (Mac/Windows) or your host IP if Logstash runs on the host.
# When using docker-compose with a logstash service, use http://logstash:9600
# LOGSTASH_URL=http://localhost:9600
